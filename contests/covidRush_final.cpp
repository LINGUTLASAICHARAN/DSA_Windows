#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> art(n), tt(n);
    for (int i = 0; i < n; i++)
    {
        cin >> art[i];
    }
    for (int i = 0; i < n; i++)
    {
        cin >> tt[i];
    }
    vector<int> treated(m); // contains count 
    vector<int> status(m); // contains day till which it is occupied
    for (int i = 0; i < n; i++)
    {
        
    }
    

    return 0;
}